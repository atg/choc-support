Regex transformation rules

    " " is replaced with "(?:\s+)"
    " ?" is replaced with "(?:\s*)"
    IDENT is replaced with (?:[a-zA-Z_][a-zA-Z0-9_]*)

Use the "rawregex" key to avoid regex transformation